import { ShortenttextPipe } from './shortenttext.pipe';

describe('ShortenttextPipe', () => {
  it('create an instance', () => {
    const pipe = new ShortenttextPipe();
    expect(pipe).toBeTruthy();
  });
});
